"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const tokenbucket_1 = __importDefault(require("tokenbucket"));
function createTokenBucket(rateLimit, globalRateTokenBucket) {
    return rateLimit ? new tokenbucket_1.default({
        interval: 'minute',
        parentBucket: globalRateTokenBucket,
        size: Math.ceil(rateLimit / 2.0),
        spread: true,
        tokensToAddPerInterval: rateLimit,
    }) : globalRateTokenBucket;
}
exports.createTokenBucket = createTokenBucket;
